#include <iostream>
using namespace std;
int main() {
    int x = 7, y = 10;
    cout << boolalpha;
    cout << "x == y ? " << (x == y) << endl;
    cout << "x != y ? " << (x != y) << endl;
    cout << "x > y ? " << (x > y) << endl;
    cout << "x < y ? " << (x < y) << endl;
    return 0;
}